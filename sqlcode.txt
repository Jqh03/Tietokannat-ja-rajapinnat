CREATE DATABASE onlinestore;
CREATE USER storeUser@"localhost" IDENTIFIED BY "storepass";
GRANT ALL ON onlinestore.* TO storeUser@"localhost";

USE onlinestore;

CREATE TABLE products(
    id_products INT PRIMARY KEY AUTO_INCREMENT, 
    name VARCHAR(20),
    price DOUBLE,
    saldo INT
);

INSERT INTO products (name,price,saldo) VALUES("TV 55",659.99, 120);
INSERT INTO products (name,price,saldo) VALUES("VASARA",59.99, 140);

CREATE TABLE customers(
    username VARCHAR(10) PRIMARY KEY,
    fname VARCHAR(20),
    lname VARCHAR(20),
    password VARCHAR(100)
);

ALTER TABLE customers ADD COLUMN role VARCHAR(15) DEFAULT "user";

UPDATE customers SET ROLE="Admin" WHERE username="user01";